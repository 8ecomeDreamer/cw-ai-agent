spring:
  application:
    name: cw-ai-agent
server:
  port: 8888
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.example.CWAIAgent.demos.web.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn
